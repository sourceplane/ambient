# ambient — live deployment manifest

> GENERATED by `flows/phases/08-docs` on 2026-08-02T12:06:37Z. Re-run that phase to
> refresh (idempotent). Hand-edits here are overwritten. Operating guide:
> [operations.md](operations.md). No secret values appear in this file —
> inventories are names only.

## Identity

| | |
|---|---|
| product / project slug | `ambient` |
| Orun workspace | `ws_91J9CD5W` (slug `ambient`) |
| declared in | `intent.yaml` → `execution.state.workspace` / `.project` |
| baseline provenance | `sourceplane/lumen@56578a5b9301eb7c649e76e19a25e5e1ad12c5f3` (see [fork-from-baseline.md](fork-from-baseline.md)) |
| CI lane CLI pin | orun `v2.52.4` (`.github/workflows/ci.yml`) |

## Public URLs (probed 2026-08-02T12:06:37Z)

| surface | URL | status |
|---|---|---|
| edge API (stage) | https://ambient-api-edge-stage.rahulvarghesepullely.workers.dev/health | LIVE (200) |
| edge API (prod) | https://ambient-api-edge-prod.rahulvarghesepullely.workers.dev/health | LIVE (200) |
| console (stage) | https://ambient-web-console-next-stage.rahulvarghesepullely.workers.dev | LIVE (200) |
| console (prod) | https://ambient-web-console-next-prod.rahulvarghesepullely.workers.dev | LIVE (200) |
| edge (custom domain) | https://api.ambient.sh/health | pending (phase 07 not run, or DNS propagating) |
| console (custom domain) | https://app.ambient.sh | pending (phase 07 not run, or DNS propagating) |

A 401/403 on `/health` still counts as deployed (alive but unauthorized);
000/5xx does not. The `dev` environment is verify-only by design — no
deployed URLs.

## Environments

- `stage` and `prod` are fully provisioned (Supabase project, Hyperdrive,
  idempotency KV, migrations) and serve the URLs above.
- `dev` is verify-only: plan/verify lanes run, nothing is deployed.

## Secrets inventory (names only)

Project rung:
  - CLOUDFLARE_ACCOUNT_ID
  - CLOUDFLARE_API_TOKEN
  - CLOUDFLARE_HYPERDRIVE_TOKEN
  - SUPABASE_ACCESS_TOKEN
  - SUPABASE_ORG_ID

Environment `stage`:
  - SUPABASE_DB_PASSWORD
  - SUPABASE_DB_URL
  - SUPABASE_PROJECT_REF
  - WIRING_CLOUDFLARE_HYPERDRIVE
  - WIRING_CLOUDFLARE_KV

Environment `prod`:
  - SUPABASE_DB_PASSWORD
  - SUPABASE_DB_URL
  - SUPABASE_PROJECT_REF
  - WIRING_CLOUDFLARE_HYPERDRIVE
  - WIRING_CLOUDFLARE_KV

Provider secrets are BROKERED (minted per-run from the workspace's
integration connections; no long-lived values at rest). `WIRING_*` and
`SUPABASE_*` env-rung entries are job-output secrets published by the
infrastructure terraform — deploy lanes resolve them for worker bindings.

## Workspace integrations

  - supabase: active
  - cloudflare: active
  - github: active

## How this deployment changes

Merges to `main` converge automatically: CI plans changed components and
runs each lane via `orun run --remote-state` against the workspace above.
See [operations.md](operations.md) for the full operating contract
(convergence, resumes, secrets, verification, troubleshooting).
